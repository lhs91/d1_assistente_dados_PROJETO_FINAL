# Núcleo LLM / agentes
langchain-google-genai==4.2.5
langgraph==1.2.4
pydantic==2.13.4

# Terminal
colorama==0.4.6

# Configuração
python-dotenv==1.2.2

# Testes
pytest==9.0.3

# Interface (Marco 4)
streamlit==1.58.0
streamlit-echarts==0.7.0

# ── LangGraph Studio / deploy local (pesado: opentelemetry, grpcio etc.) ──
# Versões TRAVADAS nas estáveis (a 0.11.0.devN quebrou /docs e o Studio).
langgraph-cli[inmem]==0.4.28
langgraph-api==0.10.0
