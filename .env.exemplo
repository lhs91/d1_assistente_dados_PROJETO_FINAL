# Copie este arquivo para .env e preencha os valores reais.
# O .env NUNCA é versionado (está no .gitignore).

# Obrigatória para usar o LLM (componentes determinísticos funcionam sem ela):
GOOGLE_API_KEY=AIzaaB6XAc
# Copie este arquivo para .env e preencha os valores reais.
# O .env NUNCA é versionado (está no .gitignore).

# Obrigatória para usar o LLM (componentes determinísticos funcionam sem ela):
GOOGLE_API_KEY=AIzaSyCy6Y5EQssdR600tZAL8S0VcuqAPaB6XAc

# Observabilidade LangSmith (projeto d1_franq).
# A LANGSMITH_API_KEY é OBRIGATÓRIA para o LangGraph Studio mostrar as runs
# (sem ela, o Studio exibe o alerta "LANGSMITH_API_KEY is missing").
LANGSMITH_API_KEY=lsv2_pt_81d2f3
LANGSMITH_TRACING=true
LANGSMITH_PROJECT="d1_franq"
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# Apenas se sua conta for da região EU, troque o endpoint acima por:
# LANGSMITH_ENDPOINT=https://eu.api.smith.langchain.com
# https://api.smith.langchain.com

